# Module 14 – STL Algorithms and Iterators

## Topics

* Iterators
* `begin()`
* `end()`
* `sort()`
* `find()`
* `count()`
* `reverse()`
* `min()`
* `max()`
* `accumulate()`
* Lambda Expressions

## Example

```cpp
sort(numbers.begin(), numbers.end());
```
