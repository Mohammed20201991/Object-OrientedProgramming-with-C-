# Lab 16 – STL Algorithms
## Objectives
* Use iterators.
* Sort data.
* Search data.
* Process collections.
## Practice
Use the following STL algorithms:
* `sort()`
* `find()`
* `count()`
* `reverse()`
* `min()`
* `max()`

## Example
```cpp id="qg8r2k"
sort(numbers.begin(), numbers.end());
```
## Challenge
Create a student ranking system using:
* `vector`
* `sort`
* Lambda expressions
